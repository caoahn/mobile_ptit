# Worker-only docker-compose for local development
# Use this when API is run directly on host machine.
services:
  redis:
    image: redis:7-alpine
    container_name: yolo-redis-worker
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis-data-worker:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - yolo-network

  worker:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        REQUIREMENTS_FILE: requirements.txt
    container_name: yolo-worker-only
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      POSTGRES_URL: ${POSTGRES_URL}
      REDIS_URL: redis://redis:6379/0
      MODEL_PATH: /app/models/yolo11n.pt
      CONFIDENCE_THRESHOLD: 0.25
      IOU_THRESHOLD: 0.45
      CELERY_TASK_TIME_LIMIT: 300
      CELERY_RESULT_EXPIRES: 3600
      TEMP_DIR: /app/temp
      LOGS_DIR: /app/logs
      LOG_LEVEL: DEBUG
      DEBUG: true
      YOLO_CONFIG_DIR: /tmp/Ultralytics
      TRANSFORMERS_CACHE: /cache/hf
    volumes:
      - .:/app
      - ./models:/app/models:ro
      - ./logs:/app/logs
      - ./temp:/app/temp
      - hf-cache-worker:/cache/hf
    depends_on:
      redis:
        condition: service_healthy
    command: celery -A services.celery_app.celery_app worker --queues=detection_jobs,embedding_jobs --loglevel=info --concurrency=2
    restart: unless-stopped
    networks:
      - yolo-network

volumes:
  redis-data-worker:
    driver: local
  hf-cache-worker:
    driver: local

networks:
  yolo-network:
    driver: bridge
