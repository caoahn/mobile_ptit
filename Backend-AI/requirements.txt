# FastAPI core
fastapi>=0.110
uvicorn[standard]>=0.29

# Redis Queue
rq>=1.15
redis>=5.0

# Image processing (headless = nhỏ hơn)
opencv-python-headless>=4.9
pillow>=10.0
numpy>=1.24

# YOLO Detection (đã bao gồm torch, torchvision)
ultralytics>=8.2

# VLM Embedding (sử dụng torch chung với ultralytics)
transformers>=4.40

# Utilities
pydantic-settings>=2.0
python-multipart>=0.0.9
requests>=2.31
aiofiles>=23.2
python-dotenv>=1.0
loguru>=0.7